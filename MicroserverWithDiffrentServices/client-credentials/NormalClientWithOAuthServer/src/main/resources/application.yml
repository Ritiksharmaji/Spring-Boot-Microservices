spring:
  application:
    name: NormalClientWithOAuthServer
  security:
    oauth2:
      client:
        registration:
          keycloak-client:
            provider: keycloak
            client-id: oauth2-client-credintials
            client-secret: mqTcUFJErUxa86LZcQTJIgkDQD7dT6D4
            authorization-grant-type: client_credentials
            scope:
              - openid

        provider:
          keycloak:
            token-uri: http://localhost:8483/realms/auth2/protocol/openid-connect/token
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8483/realms/auth2
service2:
  url: http://localhost:8082

server:
  port: 8080


