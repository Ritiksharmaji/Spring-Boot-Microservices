spring:
  application:
    name: pure-client
  security:
    oauth2:
      client:
        registration:
          keycloak-client:
            provider: keycloak
            client-id: oauth2-client-credintials
            client-secret: 0KrSijVTimVlYyau3iGFWjGKWDpcNRvL
            authorization-grant-type: client_credentials
            scope:
              - openid

        provider:
          keycloak:
            token-uri: http://localhost:8483/realms/auth2/protocol/openid-connect/token
service2:
  url: http://localhost:8082

server:
  port: 8081
